Scaling a rectangle
===================

In the code example below, we create a rectangle, and then create another
rectangle that is twice the size of the first. You can also find the same code
in the file `examples/scaling_a_rectangle.py` of the repository.

.. literalinclude:: ../examples/scaling_a_rectangle.py
